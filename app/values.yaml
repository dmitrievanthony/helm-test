app:
  name: default-app-name
  group: default-app-group
  replicaCount: 1
  container:
    image: add-imange-here
    port: 8080
    env: []
  service:
    type: ClusterIP
    port: 8080

test:
  enabled: true
  connection:
    endpoint: '/'
